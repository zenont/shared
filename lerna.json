{
  "packages": [
    "packages/*"
  ],
  "version": "0.0.1",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "allowBranch": [
        "feature/**",
        "release/**",
        "prerelease/**"
      ],
      "message": "chore(release): publish %s"
    },
    "version": {
      "allowBranch": [
        "feature/**",
        "release/**",
        "prerelease/**"
      ],
      "message": "chore(bump): bump version %s"
    }
  }
}
